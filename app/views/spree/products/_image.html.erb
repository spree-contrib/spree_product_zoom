<% if image %> 
  <%= image_tag(image.attachment.url(:product), :itemprop => "image") %>
  <br />
  <%= link_to image_tag('zoom.gif'), image.attachment.url(:original), :class => 'fancybox' %>
<% else %>
  <%= product_image(@product, :itemprop => "image") %>
  <br />
  <%= link_to image_tag('zoom.gif'), @product.images.first.attachment.url(:original), :class => 'fancybox' %>
<% end %>
